apiVersion: v1
kind: Service
metadata:
  name: {{ include "mealchart.fullname" . }}
  labels:
    {{- include "mealchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mealchart.selectorLabels" . | nindent 4 }}
